// *** DO NOT EDIT. GENERATED BY PRISMIX. ***
datasource db {
	provider = "postgresql"
	url = env("DATABASE_URL")
}
generator client {
	provider = "prisma-client-js"
	previewFeatures = ["filterJson"]
}
model Accounts {
	id String @id @default(uuid())
	createdAtUtc DateTime? @default(now()) @map("created_at_utc")
	createdBy String? @map("created_by")
	updatedAtUtc DateTime @updatedAt @map("updated_at_utc")
	updatedBy String? @map("updated_by")
	isDeleted Boolean @default(false) @map("is_deleted")
	loginMethod String @map("login_method")
	firstLoginAtUtc DateTime @default(now()) @map("first_login_at_utc")
	lastLoginAtUtc DateTime @map("last_login_at_utc")
	accessToken String @map("access_token")
	refreshToken String @map("refresh_token")
	username String 
}
model Users {
	id String @id @default(uuid())
	createdAtUtc DateTime? @default(now()) @map("created_at_utc")
	createdBy String? @map("created_by")
	updatedAtUtc DateTime @updatedAt @map("updated_at_utc")
	updatedBy String? @map("updated_by")
	isDeleted Boolean @default(false) @map("is_deleted")
	accountId String @map("account_id")
	email String 
	phone String? 
	address String? 
	timezoneOffsetInMins Int? @map("timezone_offset_in_mins")
	isOnboardingCompleted Boolean @map("is_onboarding_completed")
}
model FamilyMembers {
	id String @id @default(uuid())
	createdAtUtc DateTime? @default(now()) @map("created_at_utc")
	createdBy String? @map("created_by")
	updatedAtUtc DateTime? @updatedAt @map("updated_at_utc")
	updatedBy String? @map("updated_by")
	isDeleted Boolean? @default(false) @map("is_deleted")
	userId String? @map("user_id")
	relationshipId String? @map("relationship_id")
	firstName String? @map("first_name")
	lastName String? @map("last_name")
	dob DateTime? 
	profileImage String? @map("profile_image")
	gender String? 
	otherInfo String? @map("other_info")
	color String? 
	personalities String? 
	interests String? 
}
model RelationshipsMaster {
	id String @id @default(uuid())
	createdAtUtc DateTime? @default(now()) @map("created_at_utc")
	createdBy String? @map("created_by")
	updatedAtUtc DateTime @updatedAt @map("updated_at_utc")
	updatedBy String? @map("updated_by")
	isDeleted Boolean @default(false) @map("is_deleted")
	relation String 
	relationType String @map("relation_type")
	isVisible Boolean @map("is_visible")
}
model Habits {
	id String @id @default(uuid())
	createdAtUtc DateTime? @default(now()) @map("created_at_utc")
	createdBy String? @map("created_by")
	updatedAtUtc DateTime @updatedAt @map("updated_at_utc")
	updatedBy String? @map("updated_by")
	isDeleted Boolean @default(false) @map("is_deleted")
	name String 
	appliesForRelation String @map("applies_for_relations")
	isVisible Boolean @map("is_visible")
}
model FamilyMemberHabits {
	id String @id @default(uuid())
	createdAtUtc DateTime? @default(now()) @map("created_at_utc")
	createdBy String? @map("created_by")
	updatedAtUtc DateTime @updatedAt @map("updated_at_utc")
	updatedBy String? @map("updated_by")
	isDeleted Boolean @default(false) @map("is_deleted")
	familyMemberId String @map("family_member_id")
	habitId String @map("habit_id")
	name String 
	appliesForRelation String @map("applies_for_relations")
}